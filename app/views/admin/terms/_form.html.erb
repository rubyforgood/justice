<%= form_for([:admin, term]) do |f| %>
  <% if term.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(term.errors.count, "error") %> prohibited this term from being saved:</h2>

      <ul>
      <% term.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <dl id="meta-top">
    <dt><label for="term-name">Name</label></dt>
      <dd>
        <%= f.text_field :name %>
      </dd>
  </dl>
  <%= f.submit %>
<% end %>
